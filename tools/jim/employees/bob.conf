# Employee: Bob — audition candidate using the newer Qwen3.6 MoE already on the M5.
# Same role/persona as Jim; only the brain differs, so this is a clean model bake-off.

EMPLOYEE_NAME="Bob"
EMPLOYEE_ROLE="Local grunt-work gofer (supervised)"

# Newer generation than Jim's qwen3:30b-a3b; MoE, nvfp4 4-bit, ~21GB, fits 48GB.
MODEL="qwen3.6:35b-a3b-nvfp4"

ESCALATION_MODEL="kimi-k3:cloud"
HOST="ricksm5.local"
PORT="11434"
PERSONA="bob.md"

TEMPERATURE="0.2"
MAX_TOKENS="2048"
