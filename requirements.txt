# VideoScan — Python top-level dependencies.
# uv resolves transitive deps to a consistent set on install; no need to pin
# every transitive package the way a `pip freeze` snapshot would.
#
# Heavy/native deps that take time on first install:
#   dlib           — compiles from source via CMake (~5–10 min on M-series)
#   torch          — large download (~200 MB)
#   opencv-python  — ~100 MB

# ---- Catalog / spreadsheet ----
openpyxl

# ---- Face detection / recognition ----
dlib
face-recognition
facenet-pytorch

# ---- ML runtimes ----
coremltools
onnx
onnx2torch
torch
torchvision

# ---- Image / video ----
opencv-python

# ---- Clustering & ML ----
hdbscan
scikit-learn

# ---- Avid bin / AAF parsing ----
pyaaf2
pyavb

# ---- Plotting / analysis ----
matplotlib
seaborn
pandas

# ---- Utilities ----
tqdm
lizard
